# pluscode-reporter

Minimal scaffold for the Arabic road‑component damage reporter.

```bash
git clone <repo_url>
cd pluscode-reporter
docker compose up -d --build
```

* API docs: `https://your.domain.example/docs`
* Front‑end: `https://your.domain.example/`
